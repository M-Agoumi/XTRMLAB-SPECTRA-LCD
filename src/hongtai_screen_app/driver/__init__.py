"""
hongtai_screen_app.driver -- the panel protocol driver.

Just one module today (hongtai_screen.py -- the serial protocol,
connection handling, and the live web-mirror server), kept as its own
subpackage since it's the one piece of this project that's genuinely
reusable outside this app (see its own docstring for a standalone
usage example).
"""
